name: "Test Ethereum Client"

on:
  pull_request:
    branches:
      - main
      - "develop-[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"
  RUST_BACKTRACE: 1
  # ZISK_REF is resolved by the ./.github/actions/zisk-ref action.
  PIL2_COMPILER_REF: develop-0.10.0
  PIL2_PROOFMAN_REF: pre-develop-1.0.0-beta
  PIL2_PROOFMAN_JS_REF: pre-develop-0.19.0
  # Block inputs to test per client (from less MGs to more MGs).
  # It is good to have shared blocks between clients for easier comparison.
  RETH_TEST_BLOCKS: "25229957 25229955 25229951"
  ETHREX_TEST_BLOCKS: "25229957 25229955 25229951"

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Cache target directory
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-clippy-target-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-clippy-target-

      - name: Install ZisK dependencies
        uses: ./.github/actions/zisk-deps

      - name: Run Clippy
        run: cargo clippy --locked --workspace --all-targets -- -D warnings

  tests:
    name: Tests
    needs: [build-zisk-cpu]
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}
    runs-on: self-hosted
    timeout-minutes: 60
    container:
      image: ubuntu:22.04
      options: --shm-size=48g
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - name: Install ZisK dependencies
        uses: ./.github/actions/zisk-deps

      - name: Run cargo test
        run: cargo test --locked -p tests

  build-zisk-cpu:
    name: Build ZisK toolchain (cpu)
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}
    runs-on: self-hosted
    timeout-minutes: 60
    container:
      image: ubuntu:22.04
      options: --shm-size=48g
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - name: Set up ZisK build environment
        uses: ./.github/actions/zisk-setup

  build-zisk-gpu:
    name: Build ZisK toolchain (gpu)
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}
    runs-on: gpu-hosted
    timeout-minutes: 60
    container:
      image: ziskvm/zisk-runner-gpu:latest
      options: --gpus all --shm-size=48g
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - name: Set up ZisK build environment
        uses: ./.github/actions/zisk-setup

  proving-key:
    name: Generate proving key
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}
    runs-on: gpu-hosted
    timeout-minutes: 120
    container:
      image: ziskvm/zisk-runner-gpu:latest
      options: --gpus all --shm-size=48g
    defaults:
      run:
        shell: bash
    env:
      WS: ${{ github.workspace }}/.zk-setup
    steps:
      - uses: actions/checkout@v4

      - name: Install ZisK dependencies
        uses: ./.github/actions/zisk-deps

      - name: Build cargo-zisk
        working-directory: ${{ env.WS }}
        run: |
          cargo build --release -p cargo-zisk --bin cargo-zisk
          mkdir -p "$HOME/.zisk/bin"
          install -m 0755 target/release/cargo-zisk "$HOME/.zisk/bin/cargo-zisk"

      - name: Generate proving key (zisk build_setup.sh)
        working-directory: ${{ env.WS }}/tools/test-env
        env:
          ZISK_GHA: "1" # skip the interactive confirm prompt
          DISABLE_ENV_CONFIRM: "1"
          DISABLE_RECURSIVE_SETUP: "1"
          ONLY_CPU: "1"
          USE_CACHE_SETUP: "1" # reuse a previously generated setup
          ZISK_REPO_DIR: ${{ env.WS }}
          PIL2_COMPILER_BRANCH: ${{ env.PIL2_COMPILER_REF }}
          PIL2_PROOFMAN_BRANCH: ${{ env.PIL2_PROOFMAN_REF }}
          PIL2_PROOFMAN_JS_BRANCH: ${{ env.PIL2_PROOFMAN_JS_REF }}
        # build_setup installs the key to $HOME/.zisk/provingKey.
        run: ./build_setup.sh

  execute:
    name: Execute ${{ matrix.client }} (${{ matrix.mode }})
    needs: [build-zisk-cpu]
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}
    runs-on: self-hosted
    timeout-minutes: 60
    container:
      image: ubuntu:22.04
      options: --shm-size=48g
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        client: [reth, ethrex]
        mode: [standard, hints]
    steps:
      - uses: actions/checkout@v4

      - name: Build host
        run: |
          if [ "${{ matrix.mode }}" = "hints" ]; then
            RUSTFLAGS="--cfg zisk_hints" cargo build --locked --release -p host
          else
            cargo build --locked --release -p host
          fi

      - name: Execute (emulator)
        if: ${{ matrix.mode == 'standard' }} # hints require the assembly executor
        run: bash tools/ci/run-validation.sh "${{ matrix.client }}" execute emulator

      - name: Execute (assembly)
        run: bash tools/ci/run-validation.sh "${{ matrix.client }}" execute assembly "${{ matrix.mode }}"

  test:
    name: Verify constraints ${{ matrix.client }}
    needs: [proving-key, build-zisk-gpu]
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}
    runs-on: gpu-hosted
    timeout-minutes: 45
    container:
      image: ziskvm/zisk-runner-gpu:latest
      options: --gpus all --shm-size=48g
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        client: [reth, ethrex]
    steps:
      - uses: actions/checkout@v4

      - name: Verify proving key present
        run: |
          [ -d "$HOME/.zisk/provingKey" ] || { echo "ERROR: proving key not found at \$HOME/.zisk/provingKey (the proving-key job installs it on the shared volume)"; exit 1; }

      - name: Build host
        run: cargo build --locked --release -p host

      - name: Verify constraints (emulator)
        run: |
          EXTRA_FLAGS="--gpu --proving-key $HOME/.zisk/provingKey" \
            bash tools/ci/run-validation.sh "${{ matrix.client }}" verify-constraints emulator

      - name: Verify constraints (assembly)
        run: |
          EXTRA_FLAGS="--gpu --proving-key $HOME/.zisk/provingKey" \
            bash tools/ci/run-validation.sh "${{ matrix.client }}" verify-constraints assembly
